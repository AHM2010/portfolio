# Ahmed Ashraf — Portfolio

A production-ready portfolio built with Next.js App Router, TypeScript, Tailwind CSS, Framer Motion, and React Icons.

## Local development

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_SITE_URL` to the final production origin before deploying so canonical, sitemap, and robots URLs point to the correct domain.
